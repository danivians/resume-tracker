-# Partial to display flash messages.

- flash.each do |key, value|
  .alert{class: "alert-#{key}"}
    %button{class: 'close', 'data-dismiss' => 'alert'} &times;
    %h3= key.capitalize
    %div= value

