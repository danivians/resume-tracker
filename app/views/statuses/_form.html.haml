= simple_form_for @status, html: { class: 'form-horizontal' } do |f|

  = f.error_notification

  = f.input :main
  = f.input :secondary
  .form-actions
    = f.button :submit, class: 'btn btn-primary'
    = link_to 'Cancel', statuses_path, class: 'form-cancel'
