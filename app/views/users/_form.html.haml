= simple_form_for @user, html: { class: 'form-horizontal' } do |f|

  = f.error_notification

  = f.input :name
  = f.input :login
  .form-actions
    = f.button :submit, class: 'btn btn-primary'
    = link_to 'Cancel', users_path, class: 'form-cancel'
