= simple_form_for @source, html: { class: 'form-horizontal' } do |f|

  = f.error_notification

  = f.input :name
  .form-actions
    = f.button :submit, class: 'btn btn-primary'
    = link_to 'Cancel', sources_path, class: 'form-cancel'
